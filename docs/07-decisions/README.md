# Decisions

Use Decision records for scope/product/implementation choices that do not
require a full ADR.

Use `../_templates/DECISION.md`.

An agent should create a **proposed** Decision rather than silently changing the
PRD.
